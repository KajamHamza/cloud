name: viral-env
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - transformers
      - xgboost
      - scikit-learn
      - pandas
      - numpy
      - azure-storage-blob
      - textblob
      - sentence-transformers
      - tqdm
      - mlflow
      - azureml-mlflow
      - pyodbc # Added for SQL logging capability in jobs

